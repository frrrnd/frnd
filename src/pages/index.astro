---
import Layout from '../layouts/Layout.astro';
---

<Layout>
	<main class="wrap index-grid">
		<section class="home-section">
			<h2>About</h2>
			<p>Brazilian designer passionate about UI and everything related to human-machine interaction. Front end as a hobby. Bass player for fun.</p><br>
			<p>To check out some of my work, see my <a href="/portfolio">portfolio</a>, my <a href="/icons">app icons</a> journey or my <a target="_blank" href="https://layers.to/fer">experiments.</a></p>
		</section>

		<section class="home-section">
			<h2>Now</h2>
			<p>Working as a freelance UX/UI designer for startups and agencies, and pursuing parallel projects like <a target="_blank" href="https://viewport-ui.design">Viewport UI.</a> Enhancing skills through practice and testing new experiences.</p>
		</section>

		<section class="home-section">
			<h2>Notes</h2>
			<p><a href="/notes">Talking to myself</a>, but not just in my head.</p>
		</section>

		<section class="home-section">
			<h2>Get in touch</h2>
			<ul>
				<li><a target="_blank" href="https://x.com/frrrnd">x.com</a></li>
				<li><a target="_blank" href="https://layers.to/fer">Layers</a></li>
				<li><a target="_blank" href="https://posts.cv/fer">Posts.cv</a></li>
				<li><a target="_blank" href="https://hi.new/fer">Email</a></li>
			</ul>
		</section>
	</main>
</Layout>
<style>
	.home-section:not(:last-child) {
		margin-bottom: 3rem;
	}

	.home-section:last-child {
		padding-bottom: 4rem;
	}

	.home-section h2 {
		font-size: var(--text-primary-size);
		font-weight: 400;
		color: var(--title-color);
		padding: 0 1rem;
	}

	.home-section p {
		padding: 0 1rem;
	}

	.home-section a:not(:has(h3)) {
		color: var(--link-primary-color);
		text-decoration: underline;
		transition: .4s;
	}

	.home-section a h3 {
		font-size: var(--text-primary-size);
		font-weight: 400;
	}

	.notas-section a {
		width: 100%;
		color: var(--text-primary-color);
		display: flex;
		flex-direction: row;
		justify-content: space-between;
		align-items: center;
		transition: .4s;
	}

	.notas-section a:hover {
		color: var(--link-primary-color);
		transition: .4s;
	}

	.notas-section article {
		transition: .4s;
		padding: 0 1rem;
		border-radius: 5px;
	}

	.notas-section article:hover {
		background-color: #f7f7f7;
		transition: .4s;
	}

	.home-section {
		animation: .4s fadeInTop forwards;
		opacity: 0;
	}

	.home-section:nth-child(1) {
		animation-delay: .2s;
	}

	.home-section:nth-child(2) {
		animation-delay: .4s;
	}

	.home-section:nth-child(3) {
		animation-delay: .6s;
	}

	.home-section:nth-child(4) {
		animation-delay: .8s;
	}

	.home-section ul {
		padding: 0 1rem;
		list-style-type: none;
		width: 100%;
		display: flex;
		flex-direction: row;
	}

	.home-section li {
		margin-bottom: 1rem;
		font-size: var(--text-primary-size);
		margin-right: 16px;

		& a {
			text-decoration: none;
			transition: .4s;
			display: block;
			width: max-content;
			text-decoration: underline;

			&:after {
				content: "\2192";
				display: inline-block;
				margin-left: 5px;
				opacity: 0;
				transition: .4s;
			}
		}
	}

</style>