---
import Grid from '../components/utils/Grid.astro';
import CopyClipboard from '@components/CopyClipboard/CopyClipboard.jsx';
import { getCommitInfo } from '../utils/commit.ts';

const { commitId, commitDate } = getCommitInfo();

function formatDate(dateString: string) {
  const date = new Date(dateString);
  const month = String(date.getMonth() + 1).padStart(2, '0'); // getMonth() começa do 0
  const day = String(date.getDate()).padStart(2, '0');
  const year = date.getFullYear();
  return `${month}.${day}.${year}`;
}
---
<footer class="footer wrap-lg">
    <section class="footer__end">
        <CopyClipboard client:load />

        <!-- <div class="footer__bottom">
            <p><a target="_blank" href="https://x.com/frrrnd">@frrrnd</a></p>
        </div> -->
    </section>

    <section class="footer__content">
        <ul>
            <li class="shuffle"><a href="/about">About</a></li>
            <li class="shuffle"><a href="/works">Works</a></li>
            <li class="shuffle"><a href="/notes">Notes</a></li>
            <li class="shuffle"><a href="/labs">Labs</a></li>
            <li class="shuffle"><a href="/my-girls">My Girls</a></li>
            <li class="shuffle"><a href="/readings">Readings</a></li>
        </ul>
    </section>

    <!-- <section class="footer__data">
        <p>Last updated {formatDate(commitDate)}</p>
        <p>{commitId}</p>
    </section> -->
        
</footer>
<style>
    .footer {
        padding-bottom: var(--spacing-2);
        padding-top: var(--spacing-8);
        font-size: 0.9rem;
    }

    .footer__content {
        margin-bottom: var(--spacing-2);
        display: flex;
        flex-direction: row;
        gap: var(--spacing-2);

        h4 {
            margin-bottom: var(--spacing-1);
            color: hsla(var(--body-text-color), 1);
        }

        ul {
            display: flex;
            flex-direction: row;
            justify-content: center;
            gap: var(--spacing-2);
            list-style-type: none;
            padding-left: 0;
            margin: 0 auto;
        }

        a {
            color: hsla(var(--body-text-color), 0.6);
        }
    }

    .footer__end {
        display: flex;
        flex-direction: column;
        margin-bottom: var(--spacing-4);
        
        p {
            font-size: 1rem;
        }
    }

    .footer__bottom {
        display: flex;
        flex-direction: row;
        justify-content: space-between;
        margin-top: var(--spacing-1);
        
        p {
            font-size: var(--font-size-small);
            font-weight: 400;
            width: 100%;
            text-align: center;
            color: hsl(var(--body-text-color), 0.3);
        }
    }


    .footer__data {
        opacity: 0.5;
        display: flex;
        flex-direction: row;
        justify-content: space-between;

        p {
            font-size: 0.8rem;
            font-weight: 300;
        }
    }

    @media screen and (max-width: 768px) {
        .footer__content {
            ul {
                flex-direction: column;
                text-align: center;
                font-size: 1.5rem;
            }
        }
    }
</style>